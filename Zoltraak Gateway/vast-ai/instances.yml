info:
  name: instances
  type: http
  seq: 1

http:
  method: GET
  url: https://console.vast.ai/api/v1/instances/
  auth:
    type: bearer
    token: todo

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
